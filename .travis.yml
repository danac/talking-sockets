language: python
python:
  - "3.4"
install:
  - pip install -r requirements.txt
script:
  - python setup.py install
  - nosetests -vv --with-coverage --cover-package=talking_sockets
after_success:
  - coveralls
